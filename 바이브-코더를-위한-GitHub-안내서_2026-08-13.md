# 바이브 코더를 위한 GitHub 안내서

> 터미널을 잘 몰라도 AI와 함께 프로젝트를 안전하게 저장하고 협업하는 법

이 문서는 Git과 GitHub를 처음 사용하는 바이브 코더를 위한 입문서다. Google Drive 같은 클라우드 저장소에 빗대어 핵심 개념을 설명하고, 실제로 사용할 수 있는 `git`·`gh` 명령과 ChatGPT Work 같은 AI 코딩 환경에 입력할 프롬프트를 함께 제공한다.

> **당황하지 마세요.** Git은 처음에는 용어가 많아 보이지만, 기본 흐름은 “변경 확인 → 저장할 변경 선택 → 기록 → GitHub에 전송 → 검토 후 합치기”다.

---

## 1. Git과 GitHub는 같은 것이 아니다

- **Git**은 컴퓨터 안에서 파일의 변경 이력을 관리하는 프로그램이다.
- **GitHub**는 Git 저장소를 인터넷에 보관하고 이슈, Pull Request, 코드 리뷰, 자동 검사 같은 협업 기능을 제공하는 서비스다.
- **GitHub CLI(`gh`)**는 웹 브라우저 대신 명령줄에서 GitHub의 저장소, 이슈, PR 등을 다루는 프로그램이다.

Git은 프로젝트의 이전 상태를 **커밋이라는 스냅숏**으로 기록하는 분산 버전 관리 시스템이다. GitHub는 Git 저장소를 호스팅하면서 협업 기능을 더한다.[1]

간단히 비유하면 다음과 같다.

| 도구 | 역할 |
|---|---|
| Git | 파일 변경을 기록하는 타임머신 |
| GitHub | 타임머신 기록을 보관하고 함께 검토하는 온라인 작업실 |
| `git` 명령 | 로컬 파일과 변경 이력을 다루는 명령 |
| `gh` 명령 | GitHub의 저장소·이슈·PR 등을 다루는 명령 |

---

## 2. Google Drive에 빗대어 이해하기

GitHub는 Google Drive처럼 파일을 인터넷에 보관할 수 있지만, 작동 방식은 다르다.

Google Drive는 보통 최신 파일을 자동으로 동기화한다. Git은 사용자가 **어떤 변경을 하나의 기록으로 남길지 선택**하고, 그 기록을 명시적으로 원격 저장소에 올린다.

| Git/GitHub 용어 | Google Drive식 비유 | 실제 의미 |
|---|---|---|
| 저장소, Repository | 프로젝트 전용 공유 폴더 | 파일과 변경 이력을 함께 담는 프로젝트 단위 |
| 로컬 저장소 | 내 컴퓨터의 작업 폴더 | 내 컴퓨터에 있는 저장소 |
| 원격 저장소 | 인터넷의 공유 폴더 | GitHub 등에 있는 저장소 |
| Working Tree | 지금 편집 중인 폴더 | 현재 파일들의 실제 상태 |
| Staging Area | 저장할 파일을 고르는 바구니 | 다음 커밋에 넣을 변경을 선택하는 공간 |
| Commit | 이름표가 붙은 버전 저장점 | 선택한 변경을 로컬 이력에 기록한 스냅숏 |
| Push | 내 저장 기록을 공유 폴더에 업로드 | 로컬 커밋을 GitHub에 전송 |
| Fetch | 공유 폴더의 새 소식만 확인 | 원격 이력을 가져오되 현재 작업에는 합치지 않음 |
| Pull | 최신 기록을 내려받아 내 작업에 반영 | 원격 변경을 가져와 현재 브랜치에 통합 |
| Branch | 원본과 분리된 작업용 사본 | 기본 작업 흐름을 건드리지 않는 독립 작업선 |
| Pull Request | “이 작업을 원본에 반영해 주세요” 요청 | 변경 비교·토론·검토·병합을 위한 제안 |
| Merge | 승인된 작업을 원본에 합치기 | 한 브랜치의 변경을 다른 브랜치에 통합 |

> **중요:** 커밋했다고 곧바로 GitHub에 올라가는 것은 아니다. 커밋은 우선 내 컴퓨터에 기록되며, `push`해야 GitHub로 전송된다.

---

## 3. 먼저 알아둘 안전 원칙

AI에게 GitHub 작업을 맡길 때는 **확인**, **로컬 변경**, **외부 변경**을 구분한다.

### 3.1 비교적 안전한 확인 명령

다음 명령은 보통 파일이나 GitHub 상태를 바꾸지 않는다.

```bash
git status
git diff
git diff --staged
git log --oneline -5
git branch --show-current
git remote -v
gh auth status
gh repo view
gh pr status
```

### 3.2 실행 전에 내용을 확인할 명령

다음 명령은 로컬 이력이나 파일 상태를 바꿀 수 있다.

```bash
git add
git commit
git switch
git pull
git merge
git restore
git revert
```

### 3.3 반드시 대상과 범위를 확인할 외부 변경 명령

다음 명령은 GitHub의 상태를 바꾼다.

```bash
git push
gh repo create
gh issue create
gh pr create
gh pr merge
gh release create
```

### 3.4 AI에게 공통으로 붙일 안전 문구

```text
먼저 현재 경로, Git 상태, 현재 브랜치, 원격 저장소를 확인해 주세요.
비밀번호·토큰·개인정보·임시 파일이 포함되었는지도 검사해 주세요.
기존 파일이나 이력을 임의로 삭제하거나 덮어쓰지 마세요.
오류나 충돌이 발생하면 추측으로 해결하지 말고 중단한 뒤 설명해 주세요.
커밋, 푸시, PR 생성, 머지는 각각 실행 전에 대상과 내용을 보여 주세요.
```

---

## 4. Git과 GitHub CLI 설치

## 4.1 설치 여부 먼저 확인하기

macOS 터미널, Windows PowerShell 또는 Windows Terminal에서 다음 명령을 실행한다.

```bash
git --version
gh --version
```

버전이 표시되면 해당 프로그램은 이미 설치되어 있다.

### AI 프롬프트

```text
이 컴퓨터에 Git과 GitHub CLI가 설치되어 있는지
`git --version`과 `gh --version`으로만 확인해 주세요.
설치되어 있지 않다면 바로 설치하지 말고,
운영체제에 맞는 설치 방법과 변경 사항을 먼저 설명해 주세요.
```

## 4.2 macOS

### Git

먼저 다음 명령을 실행한다.

```bash
git --version
```

Git이 없다면 macOS가 Xcode Command Line Tools 설치를 안내할 수 있다. Git 공식 사이트의 macOS 설치 프로그램을 사용할 수도 있다.[5]

Homebrew가 이미 설치되어 있다면 다음 방법도 사용할 수 있다.

```bash
brew install git
```

> Homebrew가 없는 초보자에게 Git 하나만을 위해 Homebrew부터 설치하도록 강요할 필요는 없다. 먼저 `git --version`으로 macOS가 제공하는 설치 흐름을 확인한다.

### GitHub CLI

Homebrew가 설치되어 있다면 다음과 같이 설치한다.

```bash
brew install gh
```

확인한다.

```bash
gh --version
```

GitHub CLI 공식 사이트는 macOS 설치 방법으로 Homebrew를 안내한다.[6]

## 4.3 Windows

PowerShell 또는 Windows Terminal에서 실행한다.

### Git

```powershell
winget install --id Git.Git -e
```

또는 Git 공식 사이트에서 Git for Windows 설치 프로그램을 내려받아 설치할 수 있다.[5]

### GitHub CLI

```powershell
winget install --id GitHub.cli -e
```

설치 후 터미널을 닫았다가 다시 열고 확인한다.

```powershell
git --version
gh --version
```

GitHub CLI 공식 사이트는 Windows 설치 방법으로 WinGet을 안내한다.[6]

### AI 프롬프트

```text
Windows에서 Git과 GitHub CLI 설치 여부를 확인해 주세요.
설치되어 있지 않다면 WinGet 사용 가능 여부를 먼저 확인하고,
제가 승인하기 전에는 설치 명령을 실행하지 마세요.
설치 후 새 터미널이 필요한지도 알려 주세요.
```

---

## 5. 처음 한 번 하는 Git 작성자 설정

커밋에는 작성자 이름과 이메일이 기록된다.

현재 설정을 먼저 확인한다.

```bash
git config --global --get user.name
git config --global --get user.email
```

값이 없다면 설정한다.

```bash
git config --global user.name "홍길동"
git config --global user.email "user@example.com"
```

위의 이름과 이메일은 예시다. 실제 이름과 본인의 이메일로 바꿔야 한다. 이메일을 공개하고 싶지 않다면 GitHub 계정 설정에서 제공하는 `noreply` 이메일 사용을 검토한다.

### AI 프롬프트

```text
현재 Git 작성자 이름과 이메일이 설정되어 있는지 확인해 주세요.
기존 값이 있으면 임의로 바꾸지 마세요.
설정값이 없다면 필요한 정보와 개인정보 공개 영향을 설명한 뒤 질문해 주세요.
제가 승인한 값만 설정하고 다시 확인해 주세요.
```

---

## 6. GitHub CLI 로그인과 자격 증명

초보자에게는 토큰을 직접 복사하는 방식보다 브라우저 로그인을 권장한다.

```bash
gh auth login --web
```

대화형 질문이 표시되면 일반적으로 다음을 선택한다.

1. 호스트: `GitHub.com`
2. Git 작업 프로토콜: `HTTPS`
3. 브라우저를 사용한 로그인
4. 브라우저에서 본인의 GitHub 계정으로 승인

GitHub CLI의 기본 인증은 웹 브라우저 기반 흐름이며, 성공하면 인증 토큰을 시스템 자격 증명 저장소에 보관한다. 적절한 자격 증명 저장소가 없으면 평문 파일로 대체될 수 있으므로 `gh auth status`에서 저장 상태를 확인한다.[4]

로그인 상태를 확인한다.

```bash
gh auth status
```

Git에서도 GitHub CLI의 인증을 사용하도록 설정한다.

```bash
gh auth setup-git
```

### AI 프롬프트

```text
GitHub CLI 로그인 상태를 `gh auth status`로 확인해 주세요.
로그인되어 있지 않으면 `gh auth login --web`을 시작하고,
제가 브라우저에서 직접 승인할 수 있도록 안내해 주세요.
비밀번호, 토큰, 인증 코드를 채팅에 출력하거나 별도 파일에 저장하지 마세요.
로그인 후 `gh auth setup-git`을 실행하고 상태를 다시 확인해 주세요.
```

### 보안 경고

- GitHub 비밀번호를 AI 대화창에 입력하지 않는다.
- 개인 액세스 토큰(PAT)을 프롬프트에 붙여 넣지 않는다.
- SSH **개인 키**를 AI 대화창이나 GitHub에 올리지 않는다.
- `.env`, 인증 파일, API 키를 커밋하지 않는다.
- 브라우저 로그인은 사용자가 직접 승인한다.
- 화면에 표시된 장치 코드는 꼭 필요한 GitHub 로그인 화면에만 입력한다.

---

## 7. 핵심 용어와 명령

## 7.1 저장소, Repository

저장소는 프로젝트의 파일과 변경 이력을 함께 보관하는 단위다. Google Drive의 프로젝트 폴더와 비슷하지만 누가, 언제, 무엇을, 왜 변경했는지 기록할 수 있다.

현재 폴더가 Git 저장소인지 확인한다.

```bash
git status
```

### AI 프롬프트

```text
현재 폴더가 Git 저장소인지 확인해 주세요.
저장소가 아니라면 초기화하지 말고,
현재 경로와 필요한 다음 단계만 설명해 주세요.
```

## 7.2 초기화, Init

일반 폴더를 Git이 관리하는 로컬 저장소로 바꾸는 작업이다.

```bash
git init -b main
```

이 명령은 현재 폴더 안에 숨김 `.git` 디렉터리를 만든다. GitHub에 원격 저장소를 생성하지는 않는다.

### AI 프롬프트

```text
현재 프로젝트 폴더를 Git 저장소로 초기화해 주세요.
기본 브랜치는 `main`으로 만들어 주세요.
초기화 전에 현재 경로와 기존 `.git` 폴더 유무를 확인하고,
이미 저장소라면 다시 초기화하지 말고 알려 주세요.
```

## 7.3 복제, Clone

GitHub에 이미 있는 저장소의 파일, 이력, 브랜치를 내 컴퓨터에 복사한다.[1]

이 문서에서 `OWNER/REPOSITORY`는 실제 GitHub의 `소유자명/저장소명`으로 바꾸어 사용한다.

```bash
git clone https://github.com/OWNER/REPOSITORY.git
```

GitHub CLI로도 복제할 수 있다.

```bash
gh repo clone OWNER/REPOSITORY
```

### AI 프롬프트

```text
GitHub의 `OWNER/REPOSITORY` 저장소를 현재 폴더 아래에 복제해 주세요.
같은 이름의 폴더가 이미 있으면 덮어쓰지 말고 중단해 주세요.
복제 후 원격 주소와 기본 브랜치를 확인해 주세요.
```

## 7.4 Working Tree, 작업 트리

현재 편집 중인 실제 파일과 폴더를 뜻한다. 에디터나 AI가 파일을 바꾸면 먼저 작업 트리의 상태가 달라진다. 아직 스테이징하거나 커밋하지 않은 변경도 여기에 존재한다.

## 7.5 Status, 현재 상태

새 파일, 수정된 파일, 삭제된 파일, 스테이징된 파일을 보여 준다.

```bash
git status
```

간단히 보려면 다음 명령을 사용한다.

```bash
git status --short
```

### AI 프롬프트

```text
변경하지 말고 현재 Git 상태만 확인해 주세요.
새 파일, 수정된 파일, 삭제된 파일, 스테이징된 파일을
초보자가 이해할 수 있도록 구분해 설명해 주세요.
```

## 7.6 Diff, 변경 내용 비교

파일 이름만 보는 것이 아니라 실제로 어떤 줄이 달라졌는지 확인한다.

아직 스테이징하지 않은 변경을 확인한다.

```bash
git diff
```

스테이징된 변경을 확인한다.

```bash
git diff --staged
```

### AI 프롬프트

```text
현재 변경 사항을 `git diff`로 확인해 주세요.
파일별 변경 의도를 요약하고,
비밀번호·토큰·개인정보·임시 파일이 포함되었는지 검사해 주세요.
아직 스테이징하거나 커밋하지 마세요.
```

## 7.7 스테이징, Staging과 Add

다음 커밋에 넣을 변경을 고르는 단계다. 저장할 변경을 임시 바구니에 담는다고 생각하면 된다.

파일 하나를 선택한다.

```bash
git add README.md
```

여러 파일을 명시적으로 선택한다.

```bash
git add README.md docs/guide.md
```

모든 변경을 선택하는 명령도 있다.

```bash
git add .
```

> `git add .`는 원하지 않은 파일이나 비밀 정보까지 담을 수 있다. 초보자는 가능하면 파일을 명시적으로 선택하고, 반드시 `git diff --staged`로 확인한다.

### AI 프롬프트

```text
이번 작업과 직접 관련된 파일만 스테이징해 주세요.
`git add .`는 사용하지 말고 파일을 하나씩 지정해 주세요.
스테이징 후 `git diff --staged`를 검토해 요약하고,
아직 커밋하지 마세요.
```

## 7.8 커밋, Commit

스테이징된 변경에 설명을 붙여 로컬 프로젝트 이력에 저장한다. GitHub 공식 문서도 커밋을 특정 시점의 프로젝트 상태를 담은 스냅숏에 비유한다.[3]

```bash
git commit -m "docs: 사용 방법 추가"
```

좋은 커밋 메시지는 변경 내용을 구체적으로 설명한다.

```text
좋음: docs: Windows 설치 방법 추가
좋음: fix: 로그인 후 잘못된 이동 경로 수정
나쁨: 수정
나쁨: 업데이트
```

### AI 프롬프트: 승인 후 커밋

```text
스테이징된 변경만 검토해 주세요.
변경 내용을 정확히 나타내는 커밋 메시지 후보를 제안하고,
제가 승인하기 전에는 커밋하지 마세요.
```

### AI 프롬프트: 지정한 내용으로 실행

```text
스테이징된 변경이 문서 수정뿐인지 먼저 확인한 뒤
`docs: GitHub CLI 설치 방법 추가`라는 메시지로 커밋해 주세요.
커밋 후 커밋 ID와 포함된 파일을 알려 주세요.
푸시는 하지 마세요.
```

## 7.9 원격 저장소, Remote와 Origin

**원격 저장소**는 GitHub처럼 네트워크를 통해 연결된 저장소다. `origin`은 보통 복제하거나 처음 연결한 원격 저장소에 붙는 관례적인 별명이다. 반드시 `origin`이어야 하는 것은 아니다.

원격 저장소를 확인한다.

```bash
git remote -v
```

원격 저장소를 처음 연결한다.

```bash
git remote add origin https://github.com/OWNER/REPOSITORY.git
```

> 원격 주소에 비밀번호나 토큰을 직접 넣지 않는다.

### AI 프롬프트

```text
현재 등록된 Git 원격 저장소를 확인해 주세요.
각 원격 이름과 가져오기·푸시 주소를 설명해 주세요.
주소를 추가하거나 변경하지 마세요.
```

## 7.10 Push, 푸시

로컬에 만든 커밋을 GitHub의 원격 저장소로 전송한다.[1]

이미 연결된 브랜치는 다음과 같이 푸시한다.

```bash
git push
```

새 브랜치를 처음 푸시할 때는 다음과 같이 연결 관계를 설정한다.

```bash
git push --set-upstream origin HEAD
```

짧은 형태도 같다.

```bash
git push -u origin HEAD
```

### AI 프롬프트

```text
현재 브랜치, 원격 저장소 주소, 푸시될 커밋 목록을 확인해 주세요.
대상 원격과 대상 브랜치를 명확히 보여 주세요.
제가 승인하기 전에는 `git push`를 실행하지 마세요.
```

## 7.11 Fetch, 가져오기만 하기

원격 저장소의 새 커밋과 브랜치 정보를 내려받되, 현재 작업 브랜치에는 합치지 않는다.

```bash
git fetch origin
```

### AI 프롬프트

```text
현재 작업 파일을 바꾸지 말고 `git fetch origin`으로
원격 저장소의 최신 정보만 가져와 주세요.
그 뒤 로컬 브랜치와 원격 브랜치의 차이를 설명해 주세요.
```

## 7.12 Pull, 풀

원격 저장소의 변경을 가져와 현재 로컬 브랜치에 통합한다. 개념적으로 가져오기와 통합이 이어지는 작업이다.[1]

```bash
git pull
```

원격과 브랜치를 명시할 수도 있다.

```bash
git pull origin main
```

### AI 프롬프트

```text
현재 작업 중인 미커밋 변경이 있는지 먼저 확인해 주세요.
안전하게 진행할 수 있을 때만 원격 `main` 브랜치의 최신 내용을 가져와 주세요.
충돌이 발생하면 임의로 해결하지 말고 충돌 파일과 원인을 알려 주세요.
```

## 7.13 브랜치, Branch

기본 작업 흐름과 분리된 작업 공간이다. 기능 추가, 버그 수정, 문서 변경 등을 `main`에 직접 적용하지 않고 별도 작업선에서 진행할 수 있다. GitHub는 관련 없는 작업을 서로 다른 브랜치로 나누는 흐름을 권장한다.[2]

현재 브랜치를 확인한다.

```bash
git branch --show-current
```

브랜치 목록을 확인한다.

```bash
git branch
```

새 브랜치를 만들면서 이동한다.

```bash
git switch -c docs/add-install-guide
```

기존 브랜치로 이동한다.

```bash
git switch main
```

### AI 프롬프트

```text
현재 브랜치에서 직접 수정하지 말고,
최신 `main`을 기준으로 `docs/add-github-guide` 브랜치를 만들어 주세요.
브랜치 생성 전에 미커밋 변경과 원격 최신 상태를 확인해 주세요.
```

## 7.14 기본 브랜치, Main

저장소의 기준이 되는 브랜치다. 흔히 `main`이라는 이름을 사용하지만 저장소에 따라 다른 이름일 수 있다. 명령을 실행하기 전에 실제 기본 브랜치를 확인해야 한다.

```bash
gh repo view --json defaultBranchRef --jq '.defaultBranchRef.name'
```

## 7.15 Issue, 이슈

버그, 개선 아이디어, 질문, 해야 할 일을 기록하고 논의하는 작업 카드다.

이슈 목록을 확인한다.

```bash
gh issue list
```

특정 이슈를 읽는다.

```bash
gh issue view 12
```

새 이슈를 만든다.

```bash
gh issue create \
  --title "Windows 설치 방법 추가" \
  --body "Windows 사용자를 위한 Git 및 GitHub CLI 설치 방법이 필요합니다."
```

### AI 프롬프트

```text
이 작업을 위한 GitHub 이슈 초안을 작성해 주세요.
제목, 배경, 해야 할 일, 완료 조건을 포함해 주세요.
초안만 보여 주고 제 승인 전에는 GitHub에 등록하지 마세요.
```

## 7.16 Pull Request, 풀 리퀘스트, PR

작업 브랜치의 변경을 기준 브랜치에 합치자고 제안하는 요청이다. 변경 내용을 비교하고 토론하며 리뷰와 자동 검사를 거쳐 병합하기 위한 공간이다.[2]

현재 브랜치에서 PR을 만든다.

```bash
gh pr create \
  --title "docs: GitHub 설치 안내 추가" \
  --body "## 변경 사항
- macOS 설치 방법 추가
- Windows 설치 방법 추가

## 확인 사항
- 명령 예제 검토
- 링크 확인"
```

현재 커밋 정보를 바탕으로 대화형 PR을 만들 수도 있다.

```bash
gh pr create --fill
```

### AI 프롬프트

```text
현재 브랜치와 기본 브랜치의 차이를 실제로 검토해 주세요.
PR 제목과 본문 초안을 작성하고 변경 요약과 검증 결과를 포함해 주세요.
대상 브랜치가 올바른지도 확인해 주세요.
초안만 보여 주고 승인 전에는 PR을 만들지 마세요.
```

## 7.17 Draft PR, 초안 PR

작업이 아직 끝나지 않았지만 변경 방향을 공유하고 일찍 피드백을 받고 싶을 때 사용하는 PR이다.

```bash
gh pr create --draft --fill
```

## 7.18 Review, 리뷰

PR의 변경 내용을 사람이 검토하는 과정이다. 전체 의견이나 특정 파일·줄에 댓글을 남길 수 있다.

```bash
gh pr view 12
gh pr diff 12
```

### AI 프롬프트

```text
PR #12를 리뷰해 주세요.
설명, 변경 파일, 실제 diff, 기존 코드와 테스트를 확인하고
버그 가능성, 보안 문제, 누락된 테스트, 문서 불일치를 구분해 알려 주세요.
GitHub에 리뷰나 댓글을 게시하지 말고 검토 결과만 작성해 주세요.
```

## 7.19 Checks와 CI

코드가 빌드되는지, 테스트와 형식 검사를 통과하는지 자동으로 확인하는 과정이다.

```bash
gh pr checks 12
```

완료될 때까지 상태를 관찰한다.

```bash
gh pr checks 12 --watch
```

### AI 프롬프트

```text
PR #12의 체크 상태를 확인해 주세요.
실패한 항목이 있으면 실제 실패 로그를 읽고 원인을 설명해 주세요.
파일 수정이나 재실행 전에 해결 방법과 영향 범위를 먼저 제안해 주세요.
```

## 7.20 Merge, 머지

한 브랜치의 변경을 다른 브랜치에 통합한다. 일반적으로 PR이 검토되고 자동 검사를 통과한 뒤 작업 브랜치를 기본 브랜치에 병합한다.[2]

준비 상태만 확인한다.

```bash
gh pr view 12

gh pr checks 12
```

승인 후 squash 방식으로 병합하고 원격 작업 브랜치를 삭제하는 예다.

```bash
gh pr merge 12 --squash --delete-branch
```

### AI 프롬프트: 병합 전 점검

```text
PR #12를 병합할 준비가 되었는지 확인해 주세요.
승인 상태, 체크 결과, 충돌 여부, 대상 브랜치, 미해결 리뷰를 보고해 주세요.
아직 병합하지 마세요.
```

### AI 프롬프트: 조건부 병합

```text
PR #12가 승인되었고 모든 필수 체크를 통과했으며 충돌이 없을 때만
squash 방식으로 병합하고 원격 작업 브랜치를 삭제해 주세요.
조건이 하나라도 충족되지 않으면 병합하지 말고 이유를 알려 주세요.
```

## 7.21 Conflict, 충돌

서로 다른 작업이 같은 부분을 다르게 수정해 Git이 자동으로 합칠 수 없는 상태다. 프로젝트가 망가졌다는 뜻이 아니라, 어느 내용을 남길지 사람의 판단이 필요하다는 뜻이다.

### AI 프롬프트

```text
충돌을 임의로 해결하지 마세요.
충돌한 파일과 양쪽 변경의 의도를 비교하고,
가능한 해결안과 각 해결안의 영향을 설명해 주세요.
제가 선택한 뒤에만 파일을 수정해 주세요.
```

## 7.22 `.gitignore`

Git이 추적하지 않아야 할 파일과 폴더를 지정하는 파일이다.

```gitignore
.env
node_modules/
.DS_Store
dist/
```

- `.env`: 비밀 설정이 들어갈 수 있는 환경 변수 파일
- `node_modules/`: 다시 설치할 수 있는 Node.js 의존성
- `.DS_Store`: macOS가 만드는 메타데이터 파일
- `dist/`: 빌드로 다시 만들 수 있는 결과물인 경우가 많음

프로젝트에 따라 필요한 항목은 달라진다.

### AI 프롬프트

```text
프로젝트의 기술 구성을 실제 파일로 확인하고 적절한 `.gitignore` 초안을 만들어 주세요.
비밀번호, 토큰, 개인정보, 빌드 결과, 의존성 폴더가 커밋되지 않도록 검토해 주세요.
기존 `.gitignore`가 있다면 덮어쓰지 말고 변경안을 먼저 보여 주세요.
```

> `.gitignore`에 추가해도 이미 커밋된 비밀 정보가 과거 이력에서 자동으로 사라지지는 않는다. 비밀 정보가 커밋됐다면 해당 자격 증명을 먼저 폐기·교체하고 이력 정리 방법을 별도로 검토한다.

## 7.23 README

저장소 첫 화면에 표시되는 프로젝트 설명 문서다. 일반적으로 `README.md`라는 Markdown 파일을 사용한다.[3]

README에는 다음 내용을 담을 수 있다.

- 프로젝트의 목적
- 주요 기능
- 설치 방법
- 사용 방법
- 문제 신고 방법
- 라이선스

## 7.24 Fork, 포크

다른 사람이나 조직의 저장소를 내 GitHub 계정 아래에 복사해 독립적으로 작업할 수 있게 만드는 기능이다. 원본에 직접 푸시할 권한이 없는 오픈 소스 기여에서 자주 사용한다.[1]

```bash
gh repo fork OWNER/REPOSITORY --clone
```

- 흔히 내 포크의 원격 이름은 `origin`이다.
- 원본 저장소에는 흔히 `upstream`이라는 원격 이름을 붙인다.

```bash
git remote -v
```

## 7.25 Upstream

문맥에 따라 두 뜻으로 쓰인다.

1. 브랜치가 기본적으로 push·pull할 상대 원격 브랜치
2. 포크 작업에서 원본 저장소에 붙인 관례적인 원격 이름

따라서 AI에게 “upstream을 갱신해 줘”라고만 하지 말고 원격 저장소와 브랜치를 확인하게 한다.

## 7.26 Revert, 되돌리는 새 기록

기존 커밋의 변경을 반대로 적용하는 **새 커밋**을 만든다. 이미 공유된 이력을 보존하면서 되돌릴 때 유용하다.

```bash
git revert COMMIT_ID
```

`COMMIT_ID`는 되돌릴 실제 커밋 ID로 바꾼다.

### AI 프롬프트

```text
되돌리려는 커밋과 이후 커밋의 관계를 먼저 확인해 주세요.
공유된 이력을 다시 쓰지 않는 `git revert` 방식이 적절한지 설명하고,
생성될 변경 내용을 보여 주세요. 승인 전에는 실행하지 마세요.
```

## 7.27 Restore와 Reset

- `git restore`는 주로 작업 파일이나 스테이징 상태를 되돌리는 데 사용한다.
- `git reset`은 스테이징이나 커밋 위치를 바꿀 수 있다.
- 특히 `git reset --hard`는 미커밋 작업을 잃게 할 수 있으므로 초보자가 의미를 모른 채 실행해서는 안 된다.

스테이징만 취소하고 작업 파일 변경은 남기는 예다.

```bash
git restore --staged README.md
```

### AI 프롬프트

```text
현재 변경을 잃지 않는 방법만 검토해 주세요.
`git reset --hard`, `git clean`, 강제 푸시는 사용하지 마세요.
무엇을 되돌리려는지와 보존될 내용을 먼저 설명해 주세요.
```

## 7.28 Tag와 Release

- **태그(Tag)**는 특정 커밋에 `v1.0.0` 같은 이름표를 붙인다.
- **릴리스(Release)**는 태그를 바탕으로 변경 설명과 설치 파일 등을 GitHub에서 배포하는 기능이다.

```bash
git tag v1.0.0
git push origin v1.0.0
```

GitHub 릴리스를 만든다.

```bash
gh release create v1.0.0 --generate-notes
```

릴리스는 외부 배포 행위이므로 버전, 대상 커밋, 공개 파일을 반드시 확인한다.

---

## 8. 원격 저장소 만들기

## 8.1 새 저장소를 만들고 내 컴퓨터에 복제하기

비공개 저장소를 생성하고 복제한다.

```bash
gh repo create my-project --private --clone
```

공개 저장소를 생성하고 복제한다.

```bash
gh repo create my-project --public --clone
```

`my-project`는 실제로 만들 저장소 이름으로 바꾼다. GitHub 공식 문서는 `gh repo create`에 공개 범위 옵션과 `--clone`을 사용하는 방법을 안내한다.[3]

### AI 프롬프트

```text
GitHub에 `my-project`라는 비공개 저장소를 만들 계획입니다.
같은 이름의 저장소가 있는지 먼저 확인하고,
생성될 소유자, 저장소 이름, 공개 범위를 보여 주세요.
제 승인 후에만 저장소를 생성하고 현재 폴더에 복제해 주세요.
```

## 8.2 이미 있는 로컬 프로젝트를 GitHub에 올리기

현재 프로젝트 폴더에서 다음 명령을 사용할 수 있다.

```bash
gh repo create my-project --private --source . --push
```

이 명령은 원격 저장소 생성과 최초 푸시를 함께 수행하므로 실행 전 검토가 특히 중요하다.

### AI 프롬프트: 사전 점검

```text
현재 프로젝트를 GitHub 비공개 저장소로 만들고 싶습니다.
먼저 다음 항목을 실제 상태를 바꾸지 않고 확인해 주세요.

1. 현재 경로와 Git 저장소 여부
2. 현재 브랜치와 커밋 상태
3. 추적·커밋·푸시될 파일 목록
4. 비밀번호, 토큰, 개인정보 포함 가능성
5. `.gitignore`의 적절성
6. 생성될 저장소의 소유자와 이름
7. 공개 범위가 `private`인지 여부

아직 저장소를 생성하거나 푸시하지 마세요.
```

### AI 프롬프트: 승인 후 실행

```text
앞에서 검토한 내용대로 `my-project` 비공개 저장소를 만들고
현재 프로젝트를 푸시해 주세요.
실행 후 원격 주소, 공개 범위, 기본 브랜치, 마지막 커밋을 다시 확인해 주세요.
```

## 8.3 Git 명령으로 직접 연결하기

GitHub에서 빈 저장소를 이미 만들었다면 다음 흐름으로 연결할 수 있다.

```bash
git init -b main
git add README.md
git commit -m "docs: add README"
git remote add origin https://github.com/OWNER/REPOSITORY.git
git push -u origin main
```

기존 로컬 프로젝트와 GitHub 저장소 양쪽에 서로 다른 초기 커밋이 있으면 이 흐름이 단순하게 진행되지 않을 수 있다. 초보자는 같은 프로젝트를 로컬과 GitHub 양쪽에서 각각 초기화하지 않는 편이 안전하다.

---

## 9. 바이브 코더를 위한 표준 GitHub Flow

GitHub Flow는 브랜치를 만들고, 변경을 커밋·푸시하고, PR에서 검토한 뒤 병합하는 가벼운 작업 방식이다.[2]

```bash
# 1. 현재 상태 확인
git status
git branch --show-current
git remote -v

# 2. 기본 브랜치 최신화
git switch main
git pull origin main

# 3. 작업 브랜치 만들기
git switch -c docs/add-install-guide

# 4. 파일 수정 후 상태와 차이 확인
git status
git diff

# 5. 필요한 파일만 스테이징
git add README.md docs/install.md

# 6. 커밋될 내용 확인
git diff --staged

# 7. 커밋
git commit -m "docs: Git과 GitHub CLI 설치 안내 추가"

# 8. GitHub에 브랜치 올리기
git push -u origin HEAD

# 9. PR 만들기
gh pr create --fill

# 10. 자동 검사 확인
gh pr checks --watch
```

### 단계별로 맡기는 권장 프롬프트

```text
다음 GitHub 작업을 단계적으로 진행해 주세요.

1. 현재 경로, Git 상태, 브랜치, 원격 저장소를 확인합니다.
2. 미커밋 변경이 있으면 임의로 삭제하거나 덮어쓰지 않습니다.
3. 실제 기본 브랜치를 확인하고 최신 상태인지 점검합니다.
4. 기본 브랜치에서 `docs/add-install-guide` 브랜치를 만듭니다.
5. 요청한 문서만 수정합니다.
6. `git diff`로 변경 내용을 검토합니다.
7. 관련 파일만 명시적으로 스테이징합니다.
8. `git diff --staged` 결과와 커밋 메시지 후보를 보여 줍니다.
9. 제 승인 전에는 커밋, 푸시, PR 생성, 머지를 하지 않습니다.

각 단계는 실제 명령 결과를 근거로 설명해 주세요.
오류나 충돌이 발생하면 추측으로 해결하지 말고 중단해 주세요.
```

---

## 10. 자주 쓰는 AI 프롬프트 모음

## 10.1 아무것도 바꾸지 않고 상태 진단

```text
이 프로젝트의 현재 Git 상태를 진단해 주세요.
파일이나 설정을 변경하지 말고 다음 항목을 확인해 주세요.

- 현재 경로와 저장소 루트
- 현재 브랜치와 기본 브랜치
- 수정·추가·삭제·스테이징된 파일
- 최근 커밋 5개
- 등록된 원격 저장소
- 로컬과 원격 브랜치의 차이
- 비밀 정보로 의심되는 추적 파일

초보자가 이해할 수 있는 말로 요약해 주세요.
```

## 10.2 변경 사항을 안전하게 커밋

```text
현재 변경 사항을 검토해 하나의 커밋으로 적절한지 판단해 주세요.
관련 없는 변경은 분리하고 비밀 정보나 생성 파일은 제외해 주세요.
스테이징할 파일 목록과 커밋 메시지 후보를 먼저 제시하세요.
제가 승인하기 전에는 스테이징하거나 커밋하지 마세요.
```

## 10.3 안전하게 푸시

```text
푸시 전에 현재 브랜치, 원격 저장소, 푸시될 커밋,
원격 브랜치와의 앞섬·뒤처짐 상태를 확인해 주세요.
강제 푸시는 사용하지 마세요.
대상과 영향을 보고한 뒤 제 승인을 기다려 주세요.
```

## 10.4 PR 초안 작성

```text
현재 브랜치와 기본 브랜치의 차이를 바탕으로 PR 초안을 작성해 주세요.
다음을 포함해 주세요.

- 변경 목적
- 주요 변경 사항
- 영향을 받는 기능
- 실제로 수행한 검증
- 수행하지 못한 검증과 남은 위험
- 관련 이슈

GitHub에는 아직 게시하지 마세요.
```

## 10.5 PR 리뷰

```text
이 PR의 설명과 실제 diff를 비교해 리뷰해 주세요.
기존 코드와 테스트도 필요한 범위에서 확인하세요.
문제는 심각도별로 구분하고 파일과 근거를 적어 주세요.
추측은 추측이라고 표시하고, 실제로 확인하지 않은 테스트를 통과했다고 말하지 마세요.
GitHub에는 댓글을 게시하지 마세요.
```

## 10.6 잘못된 작업 복구

```text
방금 수행한 Git 작업을 되돌리고 싶습니다.
먼저 현재 상태와 최근 명령의 영향을 확인해 주세요.
미커밋 변경과 이미 공유된 커밋을 보존하는 복구안을 우선 제시하세요.
`git reset --hard`, `git clean`, 강제 푸시는 사용하지 마세요.
승인 전에는 복구 명령을 실행하지 마세요.
```

---

## 11. 초보자가 특히 조심할 명령

다음 명령은 필요할 수 있지만 의미를 모른 채 복사해서 실행해서는 안 된다.

```bash
git reset --hard
git clean -fd
git push --force
git push --force-with-lease
git branch -D BRANCH_NAME
git checkout -- FILE
git restore FILE
rm -rf .git
```

이 명령들은 작업 파일, 미추적 파일, 브랜치, 로컬 이력 또는 원격 이력을 잃게 할 수 있다.

### AI에게 금지 사항을 명시하는 문구

```text
데이터 손실 가능성이 있는 명령은 실행하지 마세요.
특히 `reset --hard`, `clean -fd`, 강제 푸시, 강제 브랜치 삭제,
`.git` 폴더 삭제는 금지합니다.
필요하다고 판단되면 이유, 보존할 데이터, 복구 가능성, 대안을 먼저 설명해 주세요.
```

---

## 12. 자주 발생하는 문제

## 12.1 `git` 또는 `gh` 명령을 찾을 수 없음

- 프로그램이 설치되지 않았을 수 있다.
- 설치 후 기존 터미널이 새 PATH를 반영하지 못했을 수 있다.
- 터미널을 닫고 다시 연 뒤 버전을 확인한다.

```bash
git --version
gh --version
```

## 12.2 GitHub 로그인이 되어 있지 않음

```bash
gh auth status
gh auth login --web
gh auth setup-git
```

## 12.3 커밋할 것이 없다고 나옴

```bash
git status
git diff
git diff --staged
```

가능한 원인은 다음과 같다.

- 파일을 실제로 저장하지 않았다.
- 변경 파일이 `.gitignore`에 포함됐다.
- 이미 같은 변경을 커밋했다.
- 다른 폴더나 저장소에서 명령을 실행했다.

## 12.4 Push가 거부됨

다른 사람이 원격 브랜치에 먼저 커밋했거나, 보호된 브랜치에 직접 푸시하려 했거나, 권한이 없을 수 있다.

```bash
git status
git branch --show-current
git remote -v
git fetch origin
```

바로 강제 푸시하지 말고 원인을 확인한다.

## 12.5 충돌이 발생함

충돌 파일을 확인한다.

```bash
git status
```

AI에게 양쪽 변경의 의도와 해결안을 비교하게 하고, 사용자가 결정한 후 수정한다.

## 12.6 실수로 파일을 스테이징함

작업 파일의 변경은 남기고 스테이징만 취소한다.

```bash
git restore --staged FILE_NAME
```

## 12.7 비밀 정보를 커밋함

1. 먼저 해당 토큰·비밀번호·키를 폐기하거나 교체한다.
2. 추가 푸시를 중단한다.
3. 저장소가 공개되었다면 이미 노출된 것으로 간주한다.
4. 현재 파일에서만 지우는 것으로 충분한지, Git 이력에서도 제거해야 하는지 검토한다.
5. `.gitignore`와 비밀 관리 방식을 보완한다.

---

## 13. 명령어 치트 시트

| 목적 | 명령 |
|---|---|
| Git 버전 확인 | `git --version` |
| GitHub CLI 버전 확인 | `gh --version` |
| GitHub 로그인 | `gh auth login --web` |
| 로그인 상태 확인 | `gh auth status` |
| Git 자격 증명 연결 | `gh auth setup-git` |
| 저장소 상태 확인 | `git status` |
| 현재 브랜치 확인 | `git branch --show-current` |
| 원격 저장소 확인 | `git remote -v` |
| 변경 내용 확인 | `git diff` |
| 스테이징된 변경 확인 | `git diff --staged` |
| 파일 스테이징 | `git add FILE_NAME` |
| 스테이징 취소 | `git restore --staged FILE_NAME` |
| 커밋 | `git commit -m "메시지"` |
| 최근 커밋 확인 | `git log --oneline -5` |
| 새 브랜치 생성·이동 | `git switch -c BRANCH_NAME` |
| 기존 브랜치로 이동 | `git switch BRANCH_NAME` |
| 원격 정보만 가져오기 | `git fetch origin` |
| 원격 변경 가져와 통합 | `git pull` |
| 새 브랜치 최초 푸시 | `git push -u origin HEAD` |
| 저장소 복제 | `gh repo clone OWNER/REPOSITORY` |
| 비공개 저장소 생성·복제 | `gh repo create NAME --private --clone` |
| 이슈 목록 | `gh issue list` |
| 이슈 보기 | `gh issue view NUMBER` |
| PR 생성 | `gh pr create --fill` |
| 초안 PR 생성 | `gh pr create --draft --fill` |
| PR 보기 | `gh pr view NUMBER` |
| PR 변경 보기 | `gh pr diff NUMBER` |
| PR 검사 확인 | `gh pr checks NUMBER` |
| PR 병합 | `gh pr merge NUMBER --squash --delete-branch` |

`FILE_NAME`, `BRANCH_NAME`, `OWNER/REPOSITORY`, `NAME`, `NUMBER`는 설명을 위한 자리표시자다. 실제 파일 이름, 브랜치 이름, 저장소, 번호로 바꾸어 사용한다.

---

## 14. 한 장으로 보는 전체 흐름

```text
파일을 수정한다
    ↓
git status와 git diff로 변경을 확인한다
    ↓
git add로 다음 기록에 넣을 변경만 선택한다
    ↓
git diff --staged로 선택 내용을 다시 확인한다
    ↓
git commit으로 로컬 이력에 기록한다
    ↓
git push로 GitHub에 전송한다
    ↓
gh pr create로 변경을 제안한다
    ↓
리뷰와 자동 검사를 확인한다
    ↓
gh pr merge로 승인된 변경을 기본 브랜치에 합친다
```

GitHub Flow는 대체로 **브랜치 생성 → 변경 → 커밋과 푸시 → PR → 리뷰와 검사 → 머지 → 작업 브랜치 삭제** 순서로 진행된다.[2]

---

## 15. 마지막 체크리스트

### 커밋 전

- [ ] 현재 저장소와 브랜치가 맞는가?
- [ ] `git status`를 확인했는가?
- [ ] `git diff`를 읽었는가?
- [ ] 관련 파일만 스테이징했는가?
- [ ] `git diff --staged`를 확인했는가?
- [ ] 비밀번호, 토큰, 개인정보가 없는가?
- [ ] 커밋 메시지가 변경 내용을 설명하는가?

### 푸시 전

- [ ] 원격 저장소 주소가 맞는가?
- [ ] 대상 브랜치가 맞는가?
- [ ] 푸시할 커밋을 확인했는가?
- [ ] 공개 저장소에 올려도 되는 내용인가?
- [ ] 강제 푸시를 사용하지 않는가?

### PR 생성 전

- [ ] 기본 브랜치와 비교한 diff를 확인했는가?
- [ ] 관련 없는 변경이 섞이지 않았는가?
- [ ] 변경 목적과 검증 결과를 설명했는가?
- [ ] 관련 이슈가 있다면 연결했는가?

### 머지 전

- [ ] 대상 브랜치가 맞는가?
- [ ] 필요한 리뷰 승인을 받았는가?
- [ ] 필수 검사를 모두 통과했는가?
- [ ] 충돌이 없는가?
- [ ] 해결되지 않은 중요한 의견이 없는가?

---

## 맺음말

바이브 코딩에서 중요한 것은 모든 Git 명령을 외우는 일이 아니다. AI가 무엇을 확인했고, 어떤 파일을 바꾸며, 어느 저장소와 브랜치에 영향을 주는지 사용자가 이해할 수 있어야 한다.

다음 다섯 가지를 습관으로 만들면 대부분의 사고를 예방할 수 있다.

1. 작업 전 `git status`를 확인한다.
2. 커밋 전 `git diff`와 `git diff --staged`를 확인한다.
3. 관련 파일만 커밋한다.
4. 푸시·PR·머지는 대상과 영향을 확인한 뒤 실행한다.
5. 충돌이나 오류가 나면 강제로 밀어붙이지 않고 원인을 먼저 확인한다.

> **당황하지 마세요. 상태를 확인하고, 변경을 읽고, 작은 단위로 기록하면 됩니다.**

## Sources

[1] https://docs.github.com/en/get-started/using-git/about-git — About Git - GitHub Docs
[2] https://docs.github.com/en/get-started/using-github/github-flow — GitHub flow - GitHub Docs
[3] https://docs.github.com/en/repositories/creating-and-managing-repositories/quickstart-for-repositories — Quickstart for repositories - GitHub Docs
[4] https://cli.github.com/manual/gh_auth_login — gh auth login - GitHub CLI Manual
[5] https://git-scm.com/book/en/v2/Getting-Started-Installing-Git — Installing Git - Pro Git
[6] https://cli.github.com — GitHub CLI
